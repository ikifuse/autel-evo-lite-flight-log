# Autel EVO Lite / Lite+ ドローン運航記録Webアプリ 設計書

## 1. 文書の位置づけ

本書は、Autel EVO Lite / Lite+向け運航記録Webアプリの正式な技術仕様書である。現行ソースとスプレッドシートテンプレートから確認できる仕様を記録し、保守、障害解析、変更判断、および同等アプリの再構築に用いる。

- 公開版: v1.0.0（Initial stable release）
- 現行アプリ内部版: `2026.09.09.3`
- ソースの唯一の正本: `src/`
- GAS配布物: 自動生成される `dist/Code.gs`
- ドキュメント体系案内: `docs/index.md`
- システム不変条件: `docs/invariants.md`
- システム構造概要: `docs/architecture.md`
- 機能追加・拡張手順: `docs/feature-guide.md`
- コード位置の索引: `docs/code-map.md`
- スプレッドシート帳票仕様: `docs/spreadsheet-spec.md`
- テスト・品質検証仕様: `docs/test-spec.md`
- 再構築・デプロイ・運用保守ガイド: `docs/rebuild-guide.md`

本書は仕様・理由・挙動を扱う。全体の目的別案内は `docs/index.md`、システム前提や変更不可ルールは `docs/invariants.md`、データフロー概要は `docs/architecture.md`、新機能追加時の手順は `docs/feature-guide.md` を参照する。帳票の詳細レイアウトは `docs/spreadsheet-spec.md`、回帰・障害テストの詳細は `docs/test-spec.md`、再構築やデプロイ手順は `docs/rebuild-guide.md` を参照する。個々の関数を探す場合はコードマップ（`docs/code-map.md`）を使い、作業上の短い規約は `AGENTS.md` に従う。

## 2. 目的と運用思想

本システムの目的は、飛行日誌に必要な記録と機体・バッテリーの保守情報を残しながら、ワンオペ現場の操作負担を抑えることである。

設計判断には次の7条件を使う。

1. 法規・安全上、本当に必要か。
2. その時点で現物またはアプリから確認できるか。
3. 自動取得または前回値の引用にできないか。
4. 必須化が現場操作を妨げないか。
5. 初期値による虚偽記録が起きないか。
6. 戻る・中断・圏外・再試行でも正確さを保てるか。
7. 最後に正式記録として不足なく残るか。

同一現場・同一目的の連続飛行では、最初の飛行前に全点検を行い、バッテリー交換時は交換に直接必要な確認へ絞る。一連の飛行後点検は最後に行う。各飛行は省略せず1行ずつ残す。クライアント入力中はスプレッドシートへ逐次書き込まず、全工程終了時にまとめて保存する。

アプリは記録支援であり、許可・承認判断、飛行可否、安全確保、機体整備を代替しない。参照資料は次のとおりである。

- [無人航空機の飛行日誌の取扱要領](https://www.mlit.go.jp/common/001599241.pdf)
- [飛行計画の通報・飛行日誌の作成](https://www.mlit.go.jp/koku/operation.html)
- [DIPS 2.0](https://www.ossportal.dips.mlit.go.jp/portal/top/?lang=ja)

## 3. 対象機体と管理対象

対象機体は2機種に固定する。

| model値 | 登録記号 | 機体累計原本 |
|---|---|---|
| `EVO Lite` | `JU3268805C02` | `点検整備記録_EVO Lite_原本` |
| `EVO Lite+` | `JU3269B165D2` | `点検整備記録_EVO Lite+_原本` |

バッテリーは `BAT_1`～`BAT_7` の7本である。モデルとBAT番号はサーバーの許可リスト外を受け付けない。

操縦者は現在、一人の固定値を初期表示する運用である。技能証明番号は未所持を前提に空欄で扱う。補助者は「なし」「過去の氏名」「新しい人を入力」から選ぶ。

## 4. 技術構成

- 実行環境: Google Apps Script Webアプリ
- 記録先: 固定IDのGoogleスプレッドシート
- UI: `HtmlService` が返す単一ページHTML/CSS/JavaScript
- サーバー呼出し: `google.script.run`
- クライアント一時保存: Web Storageの `localStorage`
- 排他: `LockService.getScriptLock()`
- 永続的な保存計画: `PropertiesService.getScriptProperties()`
- 応答補助キャッシュ: `CacheService.getScriptCache()`
- DATE/BAT保存先識別: Script Properties上のfixed commit planとactive reservation
- 外部通信: GPS座標の逆ジオコーディングに国土地理院APIを使用

タイムゾーンは `Asia/Tokyo` である。スプレッドシートID、テンプレート名、モデル、BAT、ブロック、検証上限などは `src/00_config.gs` に集約する。

## 5. ソース構成と依存関係

ソースの正本は `src/` と `src/web/`、配布物は自動生成される `dist/Code.gs` である。ファイル別の責務・公開RPC・GAS API境界は [docs/architecture.md](docs/architecture.md)、不具合ごとの最初の2〜4ファイルは [docs/code-map.md](docs/code-map.md) を参照する。構成表をこの総合仕様書へ重複させず、両文書と結合manifestで管理する。

サーバーは `12_commit_engine.gs` を保存入口とし、codec・runtime・adapter・capture、validation/policy、plan/store/identity/retention、compare/integrity/recovery/diagnostics、帳票/BAT/正式累計/app stateへ責務を分離している。`13_legacy_compat.gs` は凍結した旧方式互換であり、現行機能から呼び出さない。

### 5.1 Webクライアントの責務分割

`33_web_engine.js` はcontroller/routerを担当する。スタイルとシェル、DOM共通処理、start/flight/postflight画面、下書き・履歴、RPC、GPS、診断UI、workflow、catalog、bootstrapはそれぞれの担当ファイルへ分離する。

画面は共通portを通して状態取得・操作を要求し、bootstrapが具体的なcontroller/画面を結び付ける。下書き形式と状態遷移は維持する。GAS・Webとも通常のJavaScriptの共有グローバルを使い、ES Modulesやファイル単位のアクセス制御は導入していない。

### 5.2 ビルド方式と互換検証

`scripts/build.mjs` は次の順で配布物を生成する。

1. `scripts/source-order.json` 順にサーバーGSを読み込み、実ファイル一覧との整合を検査する。
2. `30_web_styles.css` と `31_web_shell.html` に、`scripts/web-source-order.json` 順のJavaScriptを組み込み、単一の `APP_HTML` を作る。bootstrapは最後に実行する。
3. 結合したWebスクリプトと配布物の構文を検査し、`dist/Code.gs` を生成する。
4. `--check` では正本から再生成した期待値と既存配布物のバイト列・SHA-256を照合する。

責務分割後の生成物はB案とバイト一致するものではない。B案 `b8e1ab6723784216b119c27d6bf9c2c540e5adb5` との互換性は、同じ入力からのplan/hash/chunk、書込み・Properties・Lock・flushの呼出順、旧pending復旧、complete後手動修正、Web状態・下書き・RPCの比較で検証する。診断UIの競合詳細JSONへのHTML escapingを明示した互換例外とする。実行方法とモックの限界は [docs/test-spec.md](docs/test-spec.md) に記載する。

開発者が編集するのは正本ソースであり、GASへ適用するのは `dist/Code.gs` だけである。ローカル検証完了と本番反映は別工程として扱う。

## 6. Webアプリの全体構造

`doGet()` は初期状態、内部版、アイコンURLをテンプレートへ安全に埋め込み、単一ページを返す。クライアントはグローバルな `STATE` を描画し、画面操作ごとに状態を更新する。運航開始後は `STATE.session` を保持し、最終保存までサーバーへの帳票書込みを行わない。

通常の状態取得結果は概ね次の形である。

```text
STATE
├─ active: 運航中か
├─ today / hasTodaySheet
├─ batteries: BAT情報
├─ aircraftTotals: 機体累計
└─ session: active=false時はnull、運航中は下記構造
```

## 7. 画面と状態遷移

### 7.1 画面一覧

| phase | 主な画面 |
|---|---|
| 運航外 | 開始画面 |
| `PRE` | 飛行前点検 |
| `PRE_ABNORMAL` | 飛行前点検の異常内容 |
| `BATTERY_CHANGE` | 交換BATの装着・ロック・状態確認 |
| `READY` | 離陸待機 |
| `FLYING` | 飛行中 |
| `LANDING` | 着陸後入力 |
| `AFTER_LANDING` | 継続、BAT交換、機体交代、終了の選択 |
| `POST_ALL` | 使用機体ごとの飛行後点検と最終保存 |

開始画面には不要な「戻る」を表示しない。運航中はクライアント内の `navigationHistory` に直前状態のスナップショットを積み、「一つ前の画面に戻る」で復元する。戻った後の再入力が現在状態を上書きする。

### 7.2 正常な流れ

```text
開始条件入力
→ 最初の機体の飛行前点検
→ 離陸待機
→ 飛行中
→ 着陸後入力
→ 続行判断
   ├─ 同じ機体・同じBATで次の離陸
   ├─ BAT交換の最小確認
   ├─ 機体交代後、新機体の飛行前点検
   └─ 全飛行終了
→ 使用した全機体の飛行後点検
→ 一括保存
→ トップ画面
```

### 7.3 session構造

再構築時に必要な主要フィールドは次のとおりである。

| 分類 | フィールド |
|---|---|
| 識別 | `draftId`, `operationDate`, `dateSheet`, `forceNewLocation` |
| 運航条件 | `model`, `currentModel`, `purpose`, `route`, `method`, `category`, `permitNo`, `inspectionLocation` |
| 人 | `pilot`, `assistant`, `cert` |
| 進行 | `phase`, `blockNo`, `flightIndex`, `navigationHistory` |
| BAT | `currentBattery`, `selectedBattery`, `selectedBatteryCycle` |
| 飛行 | `startedAt`, `flights`, `totalMinutes` |
| 点検 | `preflightChecks`, `aircrafts` |
| 入力復元 | `pendingFlightInput`, `pendingBatteryChangeInput`, `pendingLandingInput`, `pendingPostflightInput` |

`aircrafts` の各要素は `model`, `used`, `preflightDone`, `flightCount`, `totalMinutes`, `preflightChecks` を持つ。各 `flight` は `model`, `index`, `battery`, `cycle`, `takeoffLocation`, `takeoffAt`, `landingLocation`, `landingAt`, `actualMinutes`, `safetyIssue`, `safetyDetail`, `batteryNote` を持つ。

## 8. 運航開始

開始画面では、機体、飛行経路・場所、飛行目的、天気・風速・風向、飛行カテゴリー、飛行方法、許可承認番号、操縦者、補助者、技能証明番号を扱う。天気・風速・風向は任意で、未選択時は空欄のまま保存する。前回条件を引用できるが、補助者は誤記防止のため自動選択しない。

`draftId` はクライアントで `op_` + UUID v4として作る。`crypto.randomUUID()` を優先し、利用できない場合は `crypto.getRandomValues()` で同形式を生成する。安全な乱数APIがなければ開始を止める。`test_...` などの旧・特別形式は本番保存入口で拒否する。「アプリテスト」も通常UUIDを使う。

## 9. 飛行方法とカテゴリー

選択肢は、通常飛行・屋内練習と、9項目の特定飛行で構成する。

| 飛行方法 | 画面上の簡易補足 |
|---|---|
| 通常飛行（特定飛行なし） | 特定飛行に該当しない通常の飛行 |
| 屋内練習 | 四方・上部が囲まれた屋内での練習 |
| 空港等周辺 | 空港周辺等 |
| 150m以上 | 地表・水面から150m以上 |
| DID | 人口集中地区 |
| 夜間 | 日没〜日の出 |
| 目視外 | 直接目視しない飛行 |
| 30m未満 | 第三者・物件から30m未満 |
| 催し場所上空 | イベント等の上空 |
| 危険物輸送 | 危険物を輸送 |
| 物件投下 | 飛行中に物件を投下 |

合計9種の特定飛行項目には、それぞれ短い補足を表示する。「通常飛行」は他項目と同時選択できない。屋内練習の排他を含む最終整合性はサーバーでも検証する。

カテゴリーⅠで特定飛行項目を選ぶとカテゴリーⅡへ自動変更し、約3秒の通知を表示する。該当項目をすべて外した場合はⅠへ戻る。カテゴリーⅢは許可・機体・操縦者等の条件を画面だけで確定できないため、自動選択しない。必要な特定飛行には、許可・承認や安全措置を確認する補足を表示する。

## 10. 飛行前点検

最初の飛行前点検は、機体へ使用BATを装着し、電源投入後に行う。画面には区分と確認内容を並べ、未確認状態から操縦者がチェックする。

1. 機体全般
2. プロペラ・フレーム
3. 通信系統
4. 推進系統
5. 電源系統
6. 自動制御系統
7. バッテリー
8. 操縦装置（プロポ）
9. 灯火
10. カメラ
11. リモートID

機体に装着中のBAT番号は必須である。現在のサイクル数はAutel Skyで確認できた場合だけ任意入力する。異常がある場合は異常内容を記録し、安全に進行できない状態では運航を中止する。

## 11. 離陸、飛行、着陸

### 11.1 BAT交換

同じ機体で継続するときは全11項目を繰り返さず、次のBATについて番号、装着・ロック、残量・温度・警告状態を確認する。機体を交代した場合は、交代先機体で最初の飛行前点検を行う。

### 11.2 離陸待機と飛行中

離陸待機ではBATと離陸場所を確認する。「離陸を記録」で離陸時刻を確定し、飛行中画面へ進む。飛行中は操縦に集中し、着陸後に記録操作を行う。

### 11.3 着陸後入力

「着陸完了」により着陸時刻を確定する。着陸場所、実飛行時間（分）、安全に影響した事項、BAT所感等を入力する。実飛行時間はAutel Skyの値を使用し、整数1～240分である。1運航全体は最大1440分、最大30飛行である。

日時はISO形式の実時刻で比較するため、23:55離陸・翌日00:10着陸のような日付またぎを許容できる。着陸は離陸以後かつ24時間以内でなければならない。帳票の時刻表示は `HH:mm`、日付シートは運航開始日を基準にする。

## 12. 機体交代と飛行後点検

EVO LiteとEVO Lite+を同じ運航内で交代できる。交代先の機体は使用済みとして登録し、その機体で初回の飛行前点検を要求する。帳票では機体ごとに別のNo.ブロックを割り当てる。

全飛行終了後、使用した各機体について次の4項目を未確認状態から点検する。

1. 機体全般
2. プロペラ・フレーム
3. 発熱
4. その他

点検実施場所、異常なし／不具合あり、不具合箇所、事象等、確認者を記録する。異常なしを初期選択しない。

## 13. 最終保存とトップ画面遷移

`submitAllPostflight()` は入力をまとめ、`finishAircraft` を1回呼ぶ。保存失敗時は現在の画面と下書きを保持する。正常応答時の責務は成功ハンドラーへ集約し、必ず次の順で処理する。

```text
STATE = res
→ render()
→ clearOperationDraft()
→ 保存完了通知
```

`res` は `active:false`, `session:null` のトップ状態である。`render()` が例外になった場合は下書きを消さず、同じ運航を再試行できる。サーバーが保存完了した後に応答だけ失われた場合も下書きが残るため、同じボタンを再度押す。サーバーはcomplete証明と署名を検証し、重複保存せずトップ状態を返す。

## 14. 固定commit plan

### 14.1 目的

Google Sheetsには複数シート・複数行をまたぐ一般的なトランザクションがない。このため、途中失敗後にロールバックするのではなく、最初に全保存先と書込内容を固定し、同じ場所へロールフォワードする。

### 14.2 保存開始前に固定するもの

- 入力署名と運航識別子
- 日付シート名、No.1 / No.2、連番番号
- ヘッダー、飛行前点検、各飛行行、飛行後点検のセル操作
- 各BAT履歴の固定行と内部識別子
- 各機体累計セル、開始値、加算値、最終値
- 各セルの保存前値、保存予定値、値種別・書式操作

計画作成時に、別の未完了draftが同じ保存先を予約していないか確認する。再試行時に新しい保存先を探さない。

### 14.3 plan状態と進捗

状態は `pending`, `writing`, `failed`, `complete`。進捗は日付帳票、BAT履歴、飛行後点検、機体累計、最終照合などの段階単位で永続化する。各セル操作は次の判定で処理する。

- 現在値が保存前値: 保存予定値を上書きする。
- 現在値が保存予定値: 既に成功済みとして進む。
- どちらでもない: 第三者変更・競合として停止する。

これにより、同じ固定セルへの再実行が冪等になる。

### 14.4 PropertiesService

計画はScript Propertiesへ保存する。METAに状態、署名、ハッシュ、チャンク数、進捗等を置き、DATAはUTF-8で最大7000バイト相当に分割する。読み出し時にバージョン、チャンク欠落、件数、SHA-256を検査し、不完全・改ざん状態では書込みを開始しない。

上限は、正規化後計画300KiB、最大44チャンク、Properties全体400KiBである。complete後は詳細チャンクを削除し、署名と応答に必要な小さな証明を30日保持する。

### 14.5 CacheService

CacheServiceはcomplete応答の高速化だけに使う補助層で、保持は最大21600秒である。取得・保存失敗は無視でき、正本はPropertiesのcomplete証明である。キャッシュ消失によって二重保存は起きない。

### 14.6 DATE/BAT ownership

DATEブロックとBAT行のownershipは、`draftId`、Script Propertiesへ永続化したfixed commit plan、`planHash`、active reservation、Script Lockを正本とする。

- 日付シート: fixed planへ`sheetName`と`blockNo`を固定し、同じpendingの再試行では再選択しない。
- BAT履歴: fixed planへ`sheetName`、`row`、`flightIndex`を固定し、同じpendingの再試行では別行へ追記しない。
- 書込み時は各対象セルの現在値をbefore/intendedと比較し、intendedなら既書込みとしてskip、beforeなら書込み、それ以外は第三者変更として競合停止する。

Spreadsheetに既に存在するDeveloper Metadataは削除しないが、新しい保存・診断・復旧・新規行選択・二重書込み防止では読み書きせず、必須条件にも使用しない。表示セルを保存完了後に利用者が空欄化した場合、将来の新しいUUIDは空き領域を再利用できる。pending中の対象セル変更と別active pendingの固定予約だけを競合として止める。

### 14.7 flushと読取検証

主要段階の書込み後に `SpreadsheetApp.flush()` を実行し、セル値・書式を読み戻す。すべての対象がfixed planどおりであることを最終確認してからcompleteへ進む。機体累計も開始値または予定最終値だけを許し、複数機体は計画順にロールフォワードする。

### 14.8 古い計画の整理

- complete証明: 30日後に削除対象。
- pending / writing / failed: 経過日数だけでは削除・failed化しない。数週間、数か月、1年後でも、計画・実データ・競合状態を再診断する。
- 安全復旧可能なら同じfixed planをロールフォワードする。
- TESTかつfixed plan対象の実データ・公式累計変更がない場合だけ、安全破棄を許可する。
- 部分保存または第三者値がある場合は削除せず、安全停止する。
- 孤立したDATAチャンクは整理対象。

## 15. 保存失敗と再試行

再試行は同じ `draftId` と同じ正規化入力でなければならない。pendingまたはfailedなら固定計画を読み、保存済み操作を検証しながら未完了段階へ進む。complete再送でも署名一致を必須とし、改変入力は拒否する。

ScriptLockは計画確認から最終確定までを覆い、最大20秒待つ。取得できなければ明確な再試行エラーを返す。異なるdraftが同じ対象へ競合した場合は自動的に別の場所へ逃がさず停止する。

T23 `AFTER_COMPLETE_BEFORE_RESPONSE` は、complete保存後・クライアント応答前の例外を模擬する。次回の同一UUID送信はcomplete証明から成功応答を返し、日付シート、BAT、機体累計を増やさない。

## 16. Spreadsheet仕様（概要）

本システムは、固定スプレッドシート内のテンプレートおよび台帳シート群と連携して記録を行う。詳細なシート構造、ブロック列範囲、セル書式、固定予約の仕様は [docs/spreadsheet-spec.md](docs/spreadsheet-spec.md) を参照すること。

- **日常点検シート（様式2）**: テンプレート `日常点検` を複製して作成（`YYYY.M.D`、同日連番 `_2`, `_3`）。1シート左右2ブロック（No.1: C〜O列 / No.2: R〜AD列）で構成され、各ブロック最大7飛行を記録する。同一機体の8飛行目や機体交代時は次の空きブロックを使用し、両方使用済みなら連番シートを生成する。
- **動的な飛行行特定**: 飛行行の開始位置は固定行番号ではなく、見出しセル「使用バッテリー」を検索して特定する。
- **TEST日付シート**: 飛行目的「アプリテスト」時のみ `TEST_YYYY.M.D` として通常シートと分離生成する。
- **バッテリー履歴（BAT_1～BAT_7）**: 13～212行を履歴領域とし、fixed planで行を固定し、before/intended/conflict照合で二重追記を防止する。
- **機体累計時間**: `点検整備記録_*_原本` のラベル「点検時の総飛行時間」に隣接するセルを動的に検出し、開始累計の取得と保存時の加算更新を行う。管理開始初期値は `00:00` である。

## 17. アプリテスト仕様

「アプリテスト」は実飛行目的ではなく、Pixel実機から本番と同一の一連の画面遷移・保存連携・障害復旧を安全に確認するための公式実行モードである。

- **目的**: 実際の飛行を行わずに、運航開始から一括保存までの全プロセスを本番環境（GAS・スプレッドシート）でエンドツーエンド検証する。
- **通常運航との重要な違い**:
  - **TESTシートへの保存**: 通常の日付シートではなく、専用の `TEST_YYYY.M.D` シートに記録し、通常業務の日誌と完全に分離する。
  - **正式機体累計の非更新**: TESTシート内では各飛行ごとに累計時間を積算表示するが、**`点検整備記録_*_原本` の正式機体累計マスターは絶対に更新しない**（例: 正式累計12:30で10分・15分のテスト時、TESTシート行は12:40, 12:55と表示されるが原本は12:30を維持）。
  - **BAT履歴の保存**: バッテリー連携の健全性を確認するため、BAT個別シートには通常どおり1行保存し、目的欄に「アプリテスト」と記録する。
  - **確認飛行の区別**: 「整備後確認飛行」「修理後確認飛行」は実飛行であるため、アプリテストではなく通常運航として正式累計へ加算する。
- **検証手順とテストケース**: アプリテストの詳細な自動検証仕様および手動削除後の再生成挙動については、[docs/test-spec.md](docs/test-spec.md) を参照すること。

## 18. 場所入力とGPS

飛行経路・場所、離陸場所、着陸場所は手入力とGPS取得の両方に対応する。GPS取得はブラウザーのGeolocation APIを高精度・8秒タイムアウトで呼び、取得した緯度経度を固定URLの国土地理院逆ジオコーディングへ送る。結果から地名を入力し、座標も保持する。他の外部サービスへ位置情報を送らない。

前回条件引用は直前運航の場所等を開始画面へ戻す。廃止済みのお気に入り場所一覧、登録、名称変更、削除のUIと専用localStorageは存在しない。

### 18.1 Spreadsheetの場所表示

飛行経路・場所、離陸場所、着陸場所は、同一の整形ルールでセルへ保存する。
- **名称のみ**: 名称を1行表示。
- **GPSのみ**: 座標を1行表示。
- **名称 ＋ GPS**: 1行目に名称、2行目に改行して `(緯度, 経度)` を配置し、文字サイズ1pt縮小・セル内折り返しを適用。

詳細な書式仕様および空白の保持ルールは [docs/spreadsheet-spec.md](docs/spreadsheet-spec.md) を参照すること。

## 19. LocalStorage

| キー | 内容 |
|---|---|
| `EVO_LITE_ACTIVE_OPERATION_V2` | 運航中のSTATE/session下書き |
| `EVO_LITE_LAST_OPERATION` | 前回条件引用用データ |
| `EVO_LITE_ASSISTANT_HISTORY_V1` | 補助者氏名履歴 |

保存失敗や再読込後に運航中下書きを復元する。旧READY状態のBATフィールドは読込時に現行構造へ補正する。localStorage操作の失敗は画面停止を避けるため安全に無視するが、端末・ブラウザーのデータ消去、プライベートモード、容量制限、WebアプリURL変更では復元できない可能性がある。

補助者履歴は氏名だけをtrim・重複除去して保存する。選択肢は「なし」、履歴氏名、「新しい人を入力」。前回の補助者は候補には加えるが、開始画面で自動選択しない。保存データの `assistant` は補助者なしなら空文字、ありなら氏名である。新しい氏名は運航開始成功後に履歴へ加える。

下書きには運航場所や点検・飛行情報が含まれるため、共有端末ではブラウザープロファイルを分ける。お気に入り場所専用キーは使用しない。

## 20. レスポンシブ設計

Pixel 6aの縦向きを基準に、ページ本体の最大幅を760pxとする。

- 430px以下: 狭幅スマートフォン用。操作要素を1列優先とし、文字・余白を詰めすぎない。
- 600px以上: 既存の2列入力グループを2列表示する。
- 761px以上: 内容幅760pxのまま画面中央へ固定する。
- 431～599px: 基本1列。

入力、ボタン、チェック、選択チップは原則44px以上の操作高を確保する。`env(safe-area-inset-top/right/bottom/left)` を余白へ加算し、iPhoneのノッチやホームインジケーターと重ならないようにする。端末判定ではなく幅で変化するため、折りたたみスマートフォンの狭い状態はスマートフォン則、広い状態は最大760px則に従う。iPad mini級でもアプリ面は760pxを超えて引き伸ばさない。

横向き専用レイアウトは設けず、縦向きを通常運用とする。レスポンシブ変更で保存データ構造や状態遷移を分岐させない。

## 21. セキュリティ

### 21.1 サーバー側入力検証

最終保存入口で、SpreadsheetまたはPropertiesの変更前に、未加工JSONと正規化後データを検証する。

| 項目 | 上限・条件 |
|---|---|
| model | EVO Lite / EVO Lite+のみ |
| battery | 整数1～7 |
| draftId | `op_`付きUUID v4 |
| 日付 | 実在日、2022～2100年 |
| 1飛行時間 | 整数1～240分 |
| 1運航合計 | 最大1440分 |
| 飛行数 | 最大30 |
| 元JSON | 最大512KiB |
| 正規化JSON | 最大128KiB |
| オブジェクト深度 | 最大8 |
| 配列長 | 最大100 |
| 元プロパティ数 | 最大5000 |
| 正規化後プロパティ数 | 最大500 |
| プロパティ名 | 最大100文字 |
| 人名 | 最大120文字 |
| 登録・証明等ID | 最大100文字 |
| 目的・方法・場所 | 各最大500文字 |
| 所感等 | 最大1000文字 |
| 詳細 | 最大2000文字 |

モデル、目的、カテゴリー、飛行方法、点検結果は許可リストで検証する。`__proto__`, `constructor`, `prototype` 等の危険キー、過剰階層、不要プロパティ大量投入を拒否する。異常入力は計画作成、日付シート作成、BAT履歴、機体累計より前に停止する。

### 21.2 Formula Injection

自由入力をセルへ保存する共通経路では、先頭の制御文字・空白・タブを読み飛ばした最初の文字が `=`, `+`, `-`, `@` の場合、RichTextValueを用いて表示文字列として保存する。これにより見た目を自然に保ち、`getFormula()`を空にする。

安全化は固定計画作成時に一度だけ行い、計画再試行では同じ予定値を使うため二重変換しない。日時、機体、BAT番号、計算済み時間等のサーバー生成値へ不要な変換を行わない。

### 21.3 HTML / XSS

状態や入力をHTML文字列へ差し込む箇所では `esc()` 相当のエスケープを通す。`doGet()` の初期JSONでは `<`, `>`, `&`, Unicode行区切りを安全なエスケープへ変換する。DOMへ追加する新機能も同じ規則に従う。

### 21.4 公開範囲

Webアプリのアクセス範囲と「次のユーザーとして実行」はGASデプロイ設定であり、リポジトリからは確認・強制できない。現在の運用目標は所有者本人だけがアクセスし、所有者権限で実行することである。デプロイ更新時に手動確認する。

スプレッドシートは監査証跡として閲覧・コメント可能、編集は所有者のみとする運用である。公開データには実名、登録記号、場所・座標が含まれ得る。非表示・保護範囲を秘密保持とみなさず、公開目的と個人情報を継続的に確認する。Spreadsheet ID単体は認証情報ではなく、実際の権限がアクセス可否を決める。

## 22. テスト方針（概要）

本システムは、GASランタイムAPIを模擬したNode.js環境で `dist/Code.gs` を対象とした自動テストスイート（`tests/regression.test.js`）を備えている。

- **基本原理**: 「障害なしで1回保存した結果」と「処理途中で障害を注入し、同一UUIDで再試行した結果」が完全に一致することを検証する。
- **基本回帰テスト（TEST 1〜15）**: 飛行数境界（3/7/8/14/15飛行）、No.1/No.2ブロック跨ぎ、連番シート、機体交代、BAT重複防止、日付またぎ、24時間超累計、UIハッシュ検証等を網羅する。
- **障害注入テスト（T16〜T23）**: 日付シート書込み後（T16）、BAT書込み後（T17/T18）、点検後（T19）、累計更新後（T20）、flush直前（T21）、complete直前（T22）、complete後・応答前（T23）の各段階での耐障害性とロールフォワードを実証する。
- **セキュリティー検証**: 数式インジェクション防止（`RichTextValue` 化と `getFormula()==''`）および異常入力の事前拒否を検証する。

各テストケースの詳細な合否基準、異常系シナリオ、および実行コマンドについては [docs/test-spec.md](docs/test-spec.md) を参照すること。

## 23. Legacy互換

`src/13_legacy_compat.gs` は旧保存処理・旧署名等の互換ヘルパーを現行処理から分離したファイルである。現行クライアントはUUID v4を使い、現行 `finishAircraft()` は固定commit plan経路を使用する。新規機能をlegacyへ追加したり、現行処理から安易に呼び戻したりしない。削除判断は参照、テスト、Git履歴、移行対象の有無を別途監査して行う。

## 24. 再構築手順（概要）

新規スプレッドシートおよびGoogle Apps Script環境へ本システムをゼロから再構築する場合は、テンプレートシートの準備、見出し文字列の配置、設定ファイルの調整、ビルド、テスト全件通過、GASデプロイという一連のステップを実施する。

詳細なStep 1〜10の手順書、原本シートの列・見出し要件、および実機検証項目については [docs/rebuild-guide.md](docs/rebuild-guide.md) を参照すること。

## 25. GASへのデプロイ（概要）

開発完了後、GAS環境へ反映する手順の概要は以下のとおりである。

1. GitHub Actionsのビルド・テスト成功を確認する。
2. 自動生成された `dist/Code.gs` の全文をGASエディタの `Code.gs` へ貼り替えて保存する。
3. 既存のWebアプリデプロイのバージョンを更新する（新規デプロイを作成するとURLが変わり、端末内の下書きや履歴が失われるため避けること）。
4. 実行ユーザー（自分）とアクセス権限を確認し、Pixel実機で更新を確認する。

詳細な操作手順と注意事項については [docs/rebuild-guide.md](docs/rebuild-guide.md) を参照すること。

## 26. 今後の変更時の注意（概要）

システム改修時は、以下の原則を厳守すること。

- 調査は `docs/code-map.md` から着手し、修正は正本である `src/` のみに行う（`dist/Code.gs` の直接修正禁止）。
- 入力項目を追加する際は、session、下書き、署名、正規化、検証、固定計画、帳票、BAT、テストの全層を一式で確認する。
- スプレッドシートの見出し文字列（「使用バッテリー」「点検時の総飛行時間」等）を変更すると動的セル検出が壊れるため厳重に注意する。
- 自由入力欄の追加時は数式インジェクション防止を適用し、保存フェーズ追加時は進捗永続化・flush・障害注入点をセットで追加する。

網羅的な安全チェックリストおよびレスポンシブ等の確認要件については [docs/rebuild-guide.md](docs/rebuild-guide.md) を参照すること。
