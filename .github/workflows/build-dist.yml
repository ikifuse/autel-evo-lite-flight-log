name: Build and verify dist Code.gs

on:
  push:
    branches:
      - main
    paths:
      - '**.[mM][dD]'
      - 'scripts/check-docs.mjs'
      - 'tests/docs-structure.test.mjs'
      - 'src/**'
      - 'dist/Code.gs'
      - 'scripts/build.mjs'
      - 'scripts/source-order.json'
      - 'scripts/web-source-order.json'
      - 'scripts/check-boundaries.mjs'
      - 'tests/regression.test.js'
      - 'tests/refactor-compat.test.js'
      - 'tests/web-compat.test.js'
      - '.github/workflows/build-dist.yml'
  pull_request:
    paths:
      - '**.[mM][dD]'
      - 'scripts/check-docs.mjs'
      - 'tests/docs-structure.test.mjs'
      - 'src/**'
      - 'dist/Code.gs'
      - 'scripts/build.mjs'
      - 'scripts/source-order.json'
      - 'scripts/web-source-order.json'
      - 'scripts/check-boundaries.mjs'
      - 'tests/regression.test.js'
      - 'tests/refactor-compat.test.js'
      - 'tests/web-compat.test.js'
      - '.github/workflows/build-dist.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Verify documentation structure and links
        run: |
          node tests/docs-structure.test.mjs
          node scripts/check-docs.mjs

      - name: Verify pull request already contains current dist
        if: github.event_name == 'pull_request'
        run: node scripts/build.mjs --check

      - name: Generate dist from src
        if: github.event_name != 'pull_request'
        run: node scripts/build.mjs

      - name: Verify generated dist
        if: github.event_name != 'pull_request'
        run: node scripts/build.mjs --check

      - name: Run regression tests against dist
        run: node tests/regression.test.js dist/Code.gs

      - name: Verify responsibility boundaries
        run: |
          node scripts/check-boundaries.mjs --self-test
          node scripts/check-boundaries.mjs

      - name: Compare server and Web behavior with the B baseline
        run: |
          node tests/refactor-compat.test.js dist/Code.gs
          node tests/web-compat.test.js dist/Code.gs

      - name: Commit generated dist to main
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
        run: |
          if [ -z "$(git status --porcelain -- dist/Code.gs)" ]; then
            echo 'dist/Code.gs is already current.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dist/Code.gs
          git commit -m 'chore: regenerate dist/Code.gs'
          git push

      - name: Verify repository copy after automatic update
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
        run: |
          git fetch origin main
          git show origin/main:dist/Code.gs > "$RUNNER_TEMP/remote-Code.gs"
          node scripts/build.mjs --check-file="$RUNNER_TEMP/remote-Code.gs"
