# テスト・品質検証仕様書

## 1. 文書の位置づけ

本書は、変更内容に応じた試験選択、合否判定、互換性の許容差、モックの限界の正本である。個々のfixture値・assertionはテストコード、設計上の品質判断は[設計第8章](../01_ドローン運航記録_設計書/08_セキュリティ・品質.md)、過去の実行結果・未確認範囲は[reports索引](index.md#過去の監査調査実装履歴)を参照する。

- 関連正本仕様書: [01設計書の目次](../01_ドローン運航記録_設計書/00_目次.md)
- スプレッドシート帳票仕様: [docs/spreadsheet-spec.md](spreadsheet-spec.md)
- コード対応索引: [docs/code-map.md](code-map.md)
- テスト実装ファイル: `tests/regression.test.js`、`tests/refactor-compat.test.js`、`tests/web-compat.test.js`
- 責務境界チェック: `scripts/check-boundaries.mjs`
- テスト実行対象: 自動生成された `dist/Code.gs`

---

## 2. テスト方針と実行環境

`tests/regression.test.js` は、保存処理で使うGAS APIをNode.js上のモックで検証する。OKレポート1件に複数の入力例・assertionを含むため、出力件数を個別入力の総数として扱わない。件数・実行環境・対象commitは実行結果から報告する。

- **対象コード**: `scripts/build.mjs` によって生成された `dist/Code.gs` を直接ロードして検証する。
- **GAS固有API境界**: モックにはGoogle Apps Script公式APIに存在するメソッドだけを実装し、`getEntireRow()`のような便利APIを追加しない。NodeモックのPASSだけを実GAS実行済みとは扱わない。
- **モックの限界**: 回帰モックの `flush()` とLock待機・解放はno-opであり、Cacheの実TTL、サービス容量・日次quota、実通信遅延、複数端末の同時実行は再現しない。Web試験もDOM・通信のモックを使用し、実ブラウザの描画やスマートフォン操作を確認する試験ではない。実GAS・実機の確認結果は対象版・日時・環境を分けて報告する。
- **検証の基本原理**:
  1. 「障害なしで1回保存した結果」と、
  2. 「処理途中で意図的に障害・例外を注入し、その後同一UUID（draftId）で再送・再試行した結果」
  の日付シート、BAT個別履歴、機体原本累計の値・書式を比較し、complete証明の成立と残DATAチャンクの消去を確認する。

---

## 3. 基本回帰テスト（TEST 1 ～ 15）

通常保存、シート切り替え境界、帳票見出し互換を検証する。番号は `tests/regression.test.js` に対応し、具体的な入力値・期待セル値は同ファイルを参照する。

| テストID | 検証内容・対象 | 期待される振る舞い・合否基準 |
|---|---|---|
| **TEST 1～3** | 単機・複数飛行・2機体 | 開始値からの行累計と機体ごとの正式累計を照合 |
| **TEST 4～7** | 使用済みブロック・8飛行・両ブロック使用済み・別現場 | [帳票の割当規則](spreadsheet-spec.md#3-日常点検シート仕様)との一致 |
| **TEST 8・13** | 複数BAT・BAT 4 | 指定BATへの履歴記録、機体・時間・正式累計の整合 |
| **TEST 9** | HH:MM変換 | 24時間・100時間境界の変換と不正分値の拒否を確認すること |
| **TEST 10** | 同UUID再送・旧累計互換 | キャッシュなし再送で二重記録せず、旧累計適用の再実行でも加算しないこと |
| **TEST 11・12** | 旧累計見出し2種 | [認識する旧見出し](spreadsheet-spec.md#31-シート生成と命名規則)で同じ出力先へ記録 |
| **TEST 14** | 操縦装置別名・飛行後点検 | 対象点検セルへチェックを記録すること |
| **TEST 15** | UIテンプレートハッシュ検証 | `src/web/` からアセンブルされた `APP_HTML` のSHA-256ハッシュが期待値と一致し、文字列の意図しない変更を検出すること |

追加UI試験は、TEST 24の旧お気に入り機能不在・直前引用/GPS/下書き機能保持、レスポンシブCSS・カテゴリー表示、補助者履歴、保存成功/失敗時の状態遷移を検査する。これらをTEST 1〜14の別名として扱わない。UIハッシュやHTML文字列の検査は、実画面の目視確認を代替しない。

---

## 4. 障害注入テスト（T16 ～ T23）

固定commit planの冪等性とロールフォワード機構を検証するため、各保存フェーズの直前・直後で強制的に例外を発生させ、同一モック内で再試行時の挙動を比較する。

| テストID | 障害注入ポイント | 再試行時の検証要件 |
|---|---|---|
| **T16** | 日付シート書込み直後 | 日付シートが既に書き込まれていることを検知し、二重書き込みせずBAT履歴へ進むこと |
| **T17** | BAT_1書込み直後 | BAT_1の現在値がfixed planのintendedと一致することを照合し、BAT_1をスキップして残りを書き込むこと |
| **T18** | BAT_2書込み直後 | BAT_1およびBAT_2を重複追加せず、残りのBATおよび点検へ進むこと |
| **T19** | 飛行後点検書込み直後 | 点検記録の完了を検証し、機体累計更新フェーズへ正しく進むこと |
| **T20** | EVO Lite累計更新後、EVO Lite+更新前 | EVO Liteの累計を二重加算せず、EVO Lite+のみを加算して整合させること |
| **T21** | 最終flush直前 | 全セル・行の計画値読み戻し検証を行い、欠落なくcompleteへ移行すること |
| **T22** | complete保存直前 | 計画の全ステップが完了していることを確認して安全にcomplete証明を発行すること |
| **T23** | complete後・クライアント応答前 | サーバー側は保存完了済み。再送時にスプレッドシートや累計を一切増やさず、complete証明から即座に成功応答を返すこと |

### 4.1 追加障害耐性試験

スイート内では上記T16〜T23に加え、以下の異常系シナリオも検証する。

- **シートコピー後の障害**: 日付シートコピー直後に停止しても、再試行で正しい帳票へ保存すること。
- **入力改変の拒否**: pending / completeの同UUIDで入力内容が変更された場合、エラーとして拒否すること。
- **セル手動変更検知**: pending計画が存在する状態で対象セルが第三者に書き換えられた場合、上書きを停止して競合エラーとすること。
- **チャンク欠落検知**: PropertiesService内の分割DATAチャンクが一部欠落・破損している場合、復旧処理を停止すること。
- **Properties・Cache障害**: DATA途中保存、進捗保存失敗、complete META確定後の圧縮中断からの再試行、Cache利用不能を検証する。
- **先行pendingと予約**: 古い順の復旧、各draft実行直前の再診断、競合時の新規保存停止、active BAT予約を検証する。

実際のLock取得失敗・同時実行競合は、このモック試験の実証範囲に含めない。

---

## 5. アプリテストの検証仕様

回帰試験では[TEST日付シート仕様](spreadsheet-spec.md#4-test日付シート仕様)と[累計更新規則](spreadsheet-spec.md#63-累計計算と更新ルール)に対し、次を組み合わせて確認する。

- 既定の気象付記あり／なしのTESTと通常運航：日付領域の分離、行累計、正式累計の非更新、飛行ごとのBAT履歴を照合する。
- TESTシートの削除後：新規UUIDの再作成と、complete済み同UUIDの無変更再送を区別して検査する。
- 通常／TESTの0-flight：点検保存が成立し、BAT履歴・正式累計を変更しないことを確認する。
- TEST保留：実データが残るpendingの破棄を拒否し、実データのない削除済み／空再作成シートの安全残骸だけを解除できることを確認する。

---

## 6. セキュリティーと入力検証仕様

### 6.1 数式インジェクション（Formula Injection）対策の検証

- 自由入力欄（飛行目的、場所、特記事項、所感等）に `=`, `+`, `-`, `@` で始まる文字列が入力された場合、セルへ `RichTextValue` として保存されること。
- シート側で `getFormula()` を呼んだ結果が必ず空文字（`""`）となり、数式として評価・実行されないことを検証する。

### 6.2 不正入力・境界値の事前拒否

以下の異常入力に対し、スプレッドシートやPropertiesServiceを1行も変更することなく、保存入口で事前拒否されることを検証する。

- 不正な機体モデル・BATの型と範囲・UUID形式。
- 許可範囲外の日付・飛行時間・運航合計。
- 飛行配列件数・payload容量・深度・プロパティ件数の上限超過。

数値・形式の契約は[architectureの入力境界](architecture.md#10-入力境界の詳細)、具体的な境界fixtureはテストコードを参照する。正常側の上限値、Properties容量不足、plan容量見積超過、同UUID冪等性も検証する。`SECURITY` レポートはこれらの回帰試験であり、全ての攻撃方法・認証設定・実GAS quotaを監査した結果ではない。

---

## 7. 責務分割の互換・境界試験

### 7.1 サーバーの基準コミット互換

`tests/refactor-compat.test.js` は、承認済みB案基準 `b8e1ab6723784216b119c27d6bf9c2c540e5adb5` のGit blobと現在の `dist/Code.gs` を、同一モック・固定日時・入力で比較する。基準オブジェクトを含むGit履歴が必要で、実GASや利用者のSpreadsheetへアクセスしない。

- 通常、8飛行、2機体交互運航、気象付きTEST、通常/TESTの0-flight、Formula Injection文字列について、plan JSON・operation順・UTF-8チャンク境界・planHash・入力署名・応答を比較する。
- セルsetter、Properties read/write/delete、flush、Lockの呼出順を比較する。実サービスの遅延や原子的実行を再現する試験ではない。
- adapterの8種類のkindについて、通常文字列とDate表現objectのsetter引数を基準版と比較する。値のDate復号が書式・配置・font・wrapへ広がらないことも確認する。
- 基準コードが残したpendingを現在コードへ移し、対象障害点から同じplanHashでroll-forwardできることを検証する。
- 基準コードのcomplete後にDATE/BAT/正式累計を手動修正し、同UUID再送が業務セルへ書かないこと、およびpendingの第三者変更を競合停止することを検証する。
- 公開GAS入口6関数とinternal関数名の末尾 `_` を検査する。

意図した機能変更では、B基準との完全同値を要求する範囲を先に見直す。基準SHAや期待値を機械的に更新して差異を隠さない。

### 7.2 Web互換

`tests/web-compat.test.js` は同じB基準の結合Webスクリプトと現在版を、NodeのVMと簡易DOM・localStorage・RPC・GPSモックで比較する。

- 起動、飛行各phase、BAT交換、機体交代、戻る操作、画面入力退避、旧下書き復元、storage不能、異常点検、0-flight、中止を比較する。
- offline時の下書き保持、確定保存payload、成功/失敗応答、描画→下書き削除→完了通知の順序、描画失敗時の下書き保持を比較する。
- 診断/復旧/破棄のRPCと表示、GPS成功・逆ジオコーディング失敗・位置情報取得失敗を比較する。
- 依存注入に伴う承認済みhandler名だけを正規化してHTMLを比較する。診断競合詳細のHTMLエスケープは承認された挙動差として個別検査し、危険なタグが文字列として表示される出力を確認する。

最終確認では `--allow-legacy-diagnostic-html` を付けない。実DOMでのスクリプト実行、レイアウト、ブラウザ権限、実ネットワーク通信はこの試験の範囲外。

### 7.2.1 分割後監査の追加安全性検証

`node tests/audit-safety.test.js` は、破損METAのDATA保持・診断と新規保存の安全停止、復旧/TEST破棄のUUID検証、未知versionの診断拒否、診断ボタンへのJavaScript注入防止を確認する。

Webで0/1/7/8/14/28/30飛行を作り、実際のRPC payloadをNode上のサーバー入口へ渡してcompleteまで確認する。`navigationHistory`は端末に保持し通信からのみ除外する。除外前後の正規化済み入力が一致することを個別検証し、Web互換比較ではこのフィールドだけを比較対象外とする。基準commitは変更せず、APP_HTMLの期待ハッシュはこの通信境界と診断ボタンのdata属性化を含む現行テンプレートを検査する。

### 7.3 責務境界とチェック自体の検証

長期再送対策は追加で `node tests/long-term-replay.test.js` を実行する。31/366/3660日後、TEST/通常運航、手動訂正・出力シート削除、署名変更拒否、complete前後障害、証明縮小の書込み前/後エラー、Properties満杯時の既存再送/新規保存停止を確認する。従来の「30日後にMETAが消える」期待値は廃止し、証明保持と無変更再送を検証する。通常保存の基準SHAは差し替えない。

`scripts/check-boundaries.mjs` は両source-orderのファイル一覧、global重複、公開GAS入口6関数、internal末尾 `_`、凍結Legacyへの逆参照、純粋なcodec/time/compareへのGAS API・非純粋依存、server/Webの直接global参照による循環を検査する。`--json` で参照元の行とsymbolを含む依存一覧を出力できる。

`--self-test` の10シナリオは、文字列・コメント・正規表現・template rawの誤検出防止、template式の参照、重複、複数可変宣言、関数/状態の循環、public/pure/Legacy境界、portsとbootstrapを検証する。

このチェックは字句解析であり、動的呼出し、計算したプロパティ名、alias、eval、HTMLイベント文字列、注入先portsの実行時経路は解決しない。local変数がglobalと同名の場合は余分な依存を報告し得る。bootstrapの結線とmutation経路は互換試験・レビューでも確認する。

---

## 8. テスト実行コマンドと合否判定

作業中は[code-map](code-map.md)で変更した責務の関連試験を選び、最終確認は次の表に従う。文書だけの構造整理にアプリ全回帰を要求しない。

| 変更対象 | 最終確認 |
|---|---|
| 文書本文・リンク・配置のみ | `node scripts/check-docs.mjs` と `node tests/docs-structure.test.mjs`。転記漏れ・意味の整合はレビューで確認 |
| 文書checker・その自己試験 | 上記2コマンド。変更した検出条件の正常／異常fixtureを確認 |
| CI設定 | 上記に加え、変更した条件分岐と既存検査が実行される経路を確認。実CI結果をローカル結果と区別 |
| `src/`・結合順・ビルド・アプリ試験 | 下記の回帰・ビルド整合・両互換・境界確認。機能変更では意図した差異と維持する互換範囲を明示 |
| 特定の安全機能・UI・計算継続 | アプリの最終確認に加え、下記各節の関連追加スイートを選択。実機・実GASが必要な確認は別途実施範囲を明記 |

すべての変更で `git diff --check` と最終差分を確認する。アプリコード変更時の基本コマンドは以下のとおり。

```bash
# 1. ソースコードの結合と構文チェック
node scripts/build.mjs

# 2. 生成された dist/Code.gs とソースの完全整合性チェック
node scripts/build.mjs --check

# 3. 回帰テスト・障害注入テストスイートの全件実行
node tests/regression.test.js

# 4. サーバー / Webの基準互換確認
node tests/refactor-compat.test.js
node tests/web-compat.test.js

# 5. 責務境界とチェック自体の確認
node scripts/check-boundaries.mjs
node scripts/check-boundaries.mjs --self-test

# 6. Git差分・空白不整合のチェック
git diff --check
```

**合否判定基準**:

- 選択した検査がすべて成功すること。アプリコード変更時は基本回帰・障害注入・追加障害・セキュリティー試験を含める。
- テスト実行結果に `FAIL` または未処理の例外が1件も存在しないこと。
- アプリコード変更時は `build.mjs --check` が成功し、`dist/Code.gs` が `src/` から忠実に生成されていること。
- ソースの構造整理ではサーバー/Web互換試験と境界チェックが成功すること。実GAS・実機で実施した確認は、Node試験とは分けて報告する。

[CI](../.github/workflows/build-dist.yml)は文書検査を実行し、差分がMarkdown・文書checker・その自己試験だけならアプリのビルド・回帰・互換・境界検査とdist自動更新を省略する。コード・CI設定を含む場合、手動実行、差分判定不能時はアプリ検査を維持する。基準Git blob参照のためcheckoutは `fetch-depth: 0` を使用する。CIの合否は対象commitの実行結果で確認し、ローカルPASSから推測しない。

## Release Candidate回帰

`node tests/release-candidate.test.js`：新規必須欄欠損、前後同名点検の区別、条件付き異常欄、0-flightのBAT検査除外、旧pending/completeの新規writer回避、進行中入力のinput/change/pagehide保存と復元、storage書込み読み戻し・例外・破損、JST日付更新、WebフォームからTEST/通常0/1/7/8/14/28/30飛行、2機体・BAT巡回・同一BAT・気象/GPS/日本語/数式風入力・境界文字数、Properties読取り失敗、各flush失敗後再送を検査する。実GAS・端末試験ではない。

Web互換試験では入力保存イベント、保持案内文、圏外capture、下書き読込み失敗警告を承認済みの挙動差として分離し、RC試験で結果を直接検査する。他の状態・通信・画面比較は保持する。regressionの帳票fixtureは、実原本で確認した後点検補足欄・BAT8見出しを含む。

## ホーム画面アイコン検証

`node tests/pwa-icons.test.js`：PNGのCRC・展開・512px RGB・metadata・Android安全円、rootコピー、画像hash、実doGet→HtmlServiceの許可meta/favicon呼出しとHTML touch link、重複・空参照を検査する。`--rc=/path/to/RC-Code.gs`を付けると、RCから保存コードとWeb JSが変わっていないことも検査する。これはGASの外側ページや実端末のホーム画面をエミュレートするものではない。

## 手修正後の計算継続の検証

`node tests/calculation-continuity.test.js`：表示式、手修正の繰返し、台帳修正、時間・回数・最新値、100時間超、不正入力保持、Date保持、導入途中失敗・再実行、無関係データ不変を検査する。既存保存回帰・互換試験とbuild一致も確認する。GoogleのFILTER/LOOKUP、実編集イベント、権限、同時実行はmockの証明範囲外。

## 文書構造の検証

`node scripts/check-docs.mjs` は次を検査する。文書の役割・新設条件は[indexの文書管理ルール](index.md#文書更新の恒久ルール)を正本とする。

- root直下の許可Markdown、docs直下の正式8文書、設計書フォルダの00目次＋8章。未許可のdocs下位Markdown・設計章は大文字拡張子も検出する。
- 00目次から全設計章、indexから正式docs・00目次・全reportsへの登録。indexへの章一覧の再掲は要求しない。
- reportsの履歴注意書き・担当正式文書へのリンクと、設計章への履歴マーカー混入。
- root直下・設計書フォルダ・docs配下のローカルファイル参照（通常リンク・画像・参照定義・HTML href/src）。コード例は除外する。

`node tests/docs-structure.test.mjs` は一時ディレクトリで正常配置、未許可Markdown、必須文書・目次／索引登録の欠落、設計章とreportsの区別、注意書き、リンク切れ、空白／括弧付きURL・各リンク構文を検証する。違反時はcheckerが対応するエラーを検出することを確かめる。CIの文書／アプリ差分判定も、workflow内の判定処理を使って検証する。

外部URLの到達性、見出しアンカー、Markdownの全構文、文書の意味や仕様転記漏れは検査範囲外で、レビューで補完する。未許可文書を通すためだけに許可一覧を広げない。
