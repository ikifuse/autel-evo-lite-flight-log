# 手修正後の計算継続：調査・実装・導入状況

## 状態

ローカル実装・ローカル検証完了。本番への導入は未実施。従前のREADME・サンプル修正とは別作業であり、ここで「実装」と記載するものを本番稼働済みとは扱わない。

## 要求

利用者が現在値を修正したら、その値を基準として以後の計算を継続する。過去の日付シートや整備記録を遡って書き換えない。毎回の修正で計算式を入れ直す作業を利用者へ要求しない。繰り返し修正が必要になった理由を、後から検討できる運用を維持する。

## 全体調査

本番の13シートについて、各シートの既存行列範囲を `userEnteredValue.formulaValue` で読み取り確認した。数式は68セル。個人情報・実運航明細をこの報告へ転記していない。

| 分類 | 現状・問題 | 対応 |
|---|---|---|
| 機体別原本の正式累計 | GASが現在値を読み、当該機体の分だけ加算。値の修正で関数は消えない | 既存処理を維持。過去飛行行への逆反映なし |
| 日常点検原本・日付/TESTシート | 調査時にセル数式なし。GASが値を記録 | 記録の手修正は保持。過去行を新しい計算元へ自動転用しない |
| 複製した点検整備記録 | 調査時にセル数式なし。手動実施記録 | 書換え・再計算なし |
| BAT_1～7 B7 | 時間累計、7式。直接上書きで計算が消える。BAT_1はD20開始、BAT_6/7はD14開始、他はD13開始 | 現在の表示値を維持し、D13:D212の以後の差分を加算する補正方式 |
| BAT_1～7 B8 | 使用回数、7式 | 修正回数＋以後の件数差分 |
| BAT_1～7 B9・E3 | 最新サイクル・最終使用日、14式 | 手修正値を保持し、参照する最新履歴が変わったらその値へ更新 |
| バッテリー台帳 F5:J11 | BAT個別シートへの参照、35式 | 台帳で値を修正した場合も対応するBAT側へ補正を適用し、参照式を戻す。J列はBAT側の膨らみ欄E4へ転記 |
| バッテリー台帳 P2:P6 | 登録本数・サイクル確認数等、5式 | 現在の修正値＋以後の集計差分。修正で分類間の数値が一致しなくなることはあり得る |

## 方式

`src/26_sheet_calculation_continuity.gs` が計算定義と編集ハンドラーを持つ。保存engine、fixed plan、planHash、Properties、pendingの形式は変更しない。新しい計算定義は `scripts/source-order.json` を通して `dist/Code.gs` に結合する。

- 非表示シート `_計算継続管理` に計算元・補正値・修正時の計算元の値・更新時刻・導入前の式と表示を保存する。利用者は通常このシートを操作しない。
- 累計・回数は「手修正値＋現在の集計－修正時の集計」。表示欄を数字で上書きしても、編集トリガーが値を補正として保持して計算式を戻す。
- 例：現在の累計70分を25分に手修正し、その後10分が追加されれば35分となる。70分を再現するために過去履歴を修正しない。
- 初回導入も現在値を補正基準にする。BAT_1/6/7の既存除外分や既存TEST分を遡及加算・減算しない。
- 以後のBAT実績集計では、目的が「アプリテスト」または既定の気象付記付きアプリテストである履歴を除外する。疑似履歴の行は既存保存仕様どおり残す。
- 利用者が独自の数式を入力した場合は上書きしない。数式自体の変更を検討した結果も尊重する。
- 日付はDateのまま保持し、UTCからの単純なシリアル換算による日付ずれを起こさない。
- 不正な入力や参照先のエラーは入力値を保持して停止する。参照エラーを0に見せかけない。
- 導入はPREPARING/PREPARED/READYを記録する。途中失敗時に導入関数を再実行しても、基準値の再初期化やトリガーの重複作成をしない。導入後に変わったセルは勝手に戻さない。

## 残る条件・矛盾

1. この方式は「差分が加わる計算の継続」であり、シートや行列の任意の構造変更を自動復元する機能ではない。BAT・原本そのものの削除、見出し削除、行列移動は参照・保存先を失わせる。意図した削除を勝手に復元しない。
2. 履歴値の修正・空欄化で現在の集計が変われば、その差分も現在値へ反映される。履歴を削除しても累計を維持したい場合は、削除後の累計を利用者が修正して新しい基準にする。過去の削除を実飛行の取消か整理かと自動推定しない。
3. 最新値の切替は履歴の行・日付・時間・サイクルの組合せで識別する。同じ行を完全に同じ内容で再利用した場合は新しい実績と識別できない。履歴削除によって以前の組合せへ戻る場合も、利用者の意図を推測しない。
4. 登録本数等を手修正すると「登録7本なのに表示8本」等の不整合は許容される。修正値を尊重するため、自動で過去や他分類を合わせ直さない。
5. Googleの編集トリガーはAPI/GASの書込みでは発火しない。GASによる履歴追加は残っている数式が再計算するが、APIで集計式自体を数値へ上書きする操作は対象外。トリガー障害・権限失効時は自動復元を保証できない。
6. 同時編集や編集直後の障害で、トリガー処理時点までに追加された履歴をどの修正基準へ含めるかは一意に決まらない。通常の保存完了後に修正する運用を維持する。
7. 本番シートの編集権限・実トリガー・Googleによる数式再計算はローカルmockでは証明できない。導入前後の実確認が必要。

## 導入

既存GASプロジェクトへ検証済みの `dist/Code.gs` を反映し、所有者がGASエディタから `installCalculationContinuity_` を一度実行する。必要なGoogle認証は所有者が行う。インストール型編集トリガーを同じプロジェクトに追加するため、新しいWebアプリURLは不要。既存Webアプリのデプロイ更新をこの機能のためだけに要求しない。

本番へ適用する前に、入力値・対応シート・独自修正の有無を再読取りする。既存の台帳参照式がすでに数値に変更されている場合は、どちらを採用するか自動推定せず停止する。

## 検証

`node tests/calculation-continuity.test.js`：表示式の計算、手修正の繰り返し、台帳修正、時間・回数・最新値、100時間超、不正入力保持、Date保持、導入途中失敗と再実行、無関係データ不変を確認。GoogleのFILTER/LOOKUP自体はmockの証明範囲外。

既存保存処理の回帰・互換試験とbuild一致も併用する。本番検証結果は実行後に追記し、予定を完了扱いしない。

公式仕様：[インストール型トリガー](https://developers.google.com/apps-script/guides/triggers/installable)、[編集イベント](https://developers.google.com/apps-script/guides/triggers/events)。

### 再開地点（2026-09-12）

- 追加 `calculation-continuity` 試験PASS。
- 既存 regression / refactor-compat / web-compat / audit-safety / long-term-replay / release-candidate / cumulative-heading / pwa-icons は全て終了コード0。
- build一致、構文を含む構造チェック、構造チェッカー自己試験PASS。
- 本番13シートに `_計算継続管理` がないことを再確認。本番への書込み・GAS反映・トリガー作成は未実施。
- Macがロック中のためApps Script画面へアクセスできず、利用者へ解除を依頼した。
- 次は、既存GASプロジェクト確認→コード反映→所有者認証が必要なら利用者操作→導入関数実行→元の表示値維持と編集イベント・Google数式再計算を実確認する。別WebアプリURLは作らない。
- commit / pushは未実施。先行するREADME・PDF・画像の未commit変更は本件に混ぜず保持している。
